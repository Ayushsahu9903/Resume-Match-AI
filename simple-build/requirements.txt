# --- Web framework ---
fastapi==0.95.2
uvicorn[standard]==0.22.0
# required by FastAPI to parse multipart/form-data (file uploads + form
# fields) used by POST /api/match.
python-multipart==0.0.9
pydantic==1.10.13

# --- Data handling ---
numpy==1.24.4

# --- Resume file parsing (POST /api/match) ---
pypdf==4.2.0
python-docx==1.1.2

# --- NLP / entity extraction ---
spacy==3.7.2
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl

# --- Machine learning / matching ---
scikit-learn==1.3.2
sentence-transformers==2.7.0

# --- Deep learning stack (backs sentence-transformers) ---
# NOTE: the Dockerfile installs torch separately from the CPU-only wheel
# index (https://download.pytorch.org/whl/cpu) to keep the image small and
# the build fast — this pin is what's used either way.
torch==2.2.2
transformers==4.39.3
huggingface-hub==0.22.2
